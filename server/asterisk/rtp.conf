[general]
; Small range on purpose: large UDP ranges are slow/flaky to publish via
; Docker Desktop on Windows (one docker-proxy process per port). 21 ports
; is far more than two hardcoded test endpoints need.
rtpstart=10000
rtpend=10020
icesupport=yes
