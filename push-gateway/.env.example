# Port the gateway listens on (Asterisk dialplan + the app both use this).
PORT=3000

# Path to the Firebase service-account JSON key (Firebase console → Project
# settings → Service accounts → Generate new private key). Without it the
# gateway runs but cannot send Android pushes.
FCM_SERVICE_ACCOUNT=./fcm-service-account.json
