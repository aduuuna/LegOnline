// Same values you've been typing into server/test-softphone.html by hand.
// The LAN IP changes with DHCP — check it's current before each test session
// (see docs/phase0.md in the repo root for how to find it).
export const SIP_CONFIG = {
  wssUrl: "ws://localhost:8088/ws",
  domain: "127.0.0.1",
};
